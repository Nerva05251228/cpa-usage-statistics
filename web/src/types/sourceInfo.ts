export type SourceInfo = { displayName: string; type: string };
export type CredentialInfo = { name: string; type: string };
export type ObservedSource = { id: string; auth_index?: string | number; source?: string; provider?: string; label?: string; type?: string; disabled?: boolean };
